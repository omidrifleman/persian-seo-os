# Tasks 001 — ezafe (tracer bullets)

1. [x] تست: import پکیج پایه بدون extras nlp موفق است و torch را نمی‌کشد
2. [x] تست: بدون backend → `EzafeBackendUnavailable` با پیام extras
3. [x] تست: mock backend → `EzafeMark` با index/token/has_ezafe/confidence
4. [x] تست: خالی/فاصله، ایموجی+مخلوط، پایداری دو بار detect، export عمومی
5. [x] پیاده `ezafe.py` با Protocol + import تنبل DadmaTools
6. [x] export در `__init__.py`؛ پین `dadmatools==2.3.6` در pyproject
7. [x] unittest کامل → ponytail full → code-review
