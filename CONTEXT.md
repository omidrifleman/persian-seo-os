# واژه‌نامه دامنه — Persian SEO OS

منبع نام‌ها: کد موجود (`packages/persian_seo_normalizer/`، `db/migrations/0001_init.sql`، `services/`).
نام اختراع‌شده مجاز نیست. ناسازگاری کد↔مستندات در انتها گزارش شده.

---

## analyze_form
**هست:** فرم canonical متن برای کلاسترینگ، تطبیق، و کلید یکتای دیتابیس (`keywords.analyze_form`). ی/ك یکسان، اعراب حذف، اعداد ASCII، ZWNJ حذف، فاصله فشرده، لاتین lowercase.
**نیست:** متن منتشرشدنی. هرگز به وردپرس/متا/تایتل نوشته نمی‌شود.

## display_form
**هست:** متن قابل انتشار: نیم‌فاصله صحیح، اعداد فارسی، نشانه‌گذاری فارسی.
**نیست:** کلید مقایسه یا fingerprint. با `analyze_form` جابه‌جا نمی‌شود.

## keyword fingerprint (`keyword_fingerprint` / ستون `fingerprint`)
**هست:** هش کوتاه SHA1 از توکن‌های `analyze_form` پس از حذف استاپ‌وردهای سئو؛ برای تشخیص کوئری‌های واقعاً یکسان (`UNIQUE (site_id, fingerprint)`).
**نیست:** نمایش کاربر، رنک، یا حجم جست‌وجو. ترتیب کلمات به‌صورت پیش‌فرض نادیده گرفته می‌شود مگر `order_sensitive=True`.

## ezafe
**هست:** کسرهٔ اضافهٔ دستوری که دو کلمه را به هم پیوند می‌دهد و معنای عبارت کلیدی را عوض می‌کند؛ در خط فارسی معمولاً نوشته نمی‌شود و باید تشخیص داده شود (`detect_ezafe` — برش فعلی).
**نیست:** نیم‌فاصله (ZWNJ)، اعراب عمومی، یا بخشی از `analyze_form`. علامت دیدنی کسره اگر درج شود به دامنهٔ نمایش تعلق دارد؛ تشخیص جدا از pipeline نرمال‌سازی پایه است.

## intent
**هست:** برچسب معنایی کوئری روی `keywords.intent` (مثلاً اطلاعاتی / ناوبری / تراکنشی) برای اولویت‌بندی محتوا.
**نیست:** fingerprint، topic cluster، یا خودِ کلیدواژه.

## topic cluster
**هست:** گروه استراتژیک از کلیدواژه‌ها/صفحات حول یک موضوع (لایه ۲ معماری).
**نیست:** هنوز جدول/تابع در کد نیست؛ با `keyword_fingerprint` یکی نیست.

## draft (`content_drafts`)
**هست:** پیش‌نویس محتوا قبل از تأیید انسان: عنوان، بدنه markdown، امتیاز کیفیت، فلگ دارایی یکتا.
**نیست:** محتوای منتشرشده؛ بدون عبور از `approvals` به `publish_jobs` نمی‌رسد.

## quality gate
**هست:** شرط کیفیت قبل از انتشار — حداقل یک عنصر غیرقابل‌تولید توسط LLM (`has_unique_asset` + `unique_asset_kind`).
**نیست:** تأیید انسانی؛ گِیت کیفیت ≠ `approvals`.

## approval (`approvals`)
**هست:** رکورد تصمیم انسان (`pending` / `approved` / `rejected`) با reviewer و چک‌لیست E-E-A-T.
**نیست:** خودِ انتشار؛ و جایگزین quality gate نیست.

## publish job (`publish_jobs`)
**هست:** واحد کار نوشتن بیرونی؛ همیشه `approval_id NOT NULL`، `idempotency_key` یکتا، `snapshot_before`، پیش‌فرض `dry_run=true`.
**نیست:** پیش‌نویس محتوا؛ بدون approval در دیتابیس قابل ایجاد نیست.

## snapshot (`snapshot_before`)
**هست:** وضعیت قبل از اعمال تغییر روی سایت مشتری؛ پیش‌نیاز rollback صادقانه.
**نیست:** نسخهٔ پیش‌نویس داخلی یا کش رندر.

## rollback (`rolled_back_at` + مسیر بازگردانی)
**هست:** بازگرداندن سایت به `snapshot_before` پس از apply.
**نیست:** حذف soft دیتابیس داخلی یا undo ویرایشگر.

## audit entry (`audit_log`)
**هست:** ردپای کامل: actor (human|agent)، زمان، action، target، reason، diff.
**نیست:** لاگ اپلیکیشن عمومی یا متریک LLM (`llm_calls` جداست).

## tenant (`tenants`)
**هست:** واحد چندمستأجری سطح بالا؛ مالک یک یا چند site.
**نیست:** کاربر انسانی، reviewer، یا دامنه.

## site (`sites`)
**هست:** یک دامنهٔ مشتری زیر tenant؛ شامل `cms` و throttle `max_posts_per_day`.
**نیست:** tenant؛ و هنوز adapter CMS پیاده نشده — فقط ستون متنی.

## تخمین حجم (estimated volume)
**هست:** فیلدهای `keywords.volume` + `volume_source`؛ در بازار فارسی دادهٔ حجم جست‌وجوی معتبر فرض نمی‌شود و باید به‌عنوان تخمین علامت‌گذاری شود.
**نیست:** حقیقت قطعی تقاضا. نام «تخمین حجم» در مستندات است؛ در اسکیما ستون جداگانه‌ای به نام `estimated_volume` وجود ندارد — فقط `volume` / `volume_source`.

---

## ناسازگاری‌های کد ↔ مستندات (گزارش)

1. مستندات/مهارت از «تخمین حجم» حرف می‌زنند؛ اسکیما فقط `volume` دارد بدون قید اجباریِ «این تخمین است».
2. `topic cluster` و `intent` در معماری آمده‌اند؛ برای cluster هنوز موجودیت کدی/جدولی نیست.
3. RLS در استثناهای ponytail دیگر «مقدسِ الان» نیست؛ به‌تعویق با شرط ADR-0003
   (اولین مستأجر خودسرویس). ستون‌های `tenant_id` می‌مانند.
4. نام تابع عمومی ezafe هنوز در کد نیست (برش فعلی آن را اضافه می‌کند: `detect_ezafe`).
