# اسکیل‌های انتخاب‌شده برای این پروژه

اصل حاکم: **اسکیل زیاد = کانتکست آلوده = ایجنت بدتر.**
هر اسکیل باید یک کار مشخص در این پروژه انجام دهد، وگرنه حذف می‌شود.

## مرحله ۱ — الان نصب کن (۱۲ تا)

### اسکلت فرایند
| اسکیل | منبع | کاربرد دقیق در این پروژه |
| --- | --- | --- |
| `spec-kit` کامل | github/spec-kit (MIT) | فرایند اصلی. `constitution` → قواعد غیرقابل‌مذاکره، `specify` → هر ماژول، `plan` → معماری، `tasks` → تکیت، `implement` → اجرا |
| `grill-me` / `grilling` | mattpocock (MIT) | سه تصمیم باز ما (CMS، مدل ارائه، محل هوست) را تا ته می‌برد. قبل از هر مرحله اجرا بزن |
| `domain-modeling` | mattpocock | واژه‌نامه دقیق: analyze_form، display_form، fingerprint، ezafe، approval. اگر این مفاهیم لغزند، کل محصول لغزیده |
| `codebase-design` | mattpocock | دقیقاً الگوی ما: رفتار زیاد پشت اینترفیس کوچک (normalizer، CMS adapter، LLM gateway) |
| `to-spec` + `to-tickets` | mattpocock | تبدیل گفتگو به تکیت‌های tracer-bullet با وابستگی مشخص |
| `handoff` | mattpocock | این پروژه چندین ماه طول می‌کشد. هر سشن باید قابل تحویل به سشن بعد باشد |

### کیفیت کد
| اسکیل | منبع | کاربرد دقیق |
| --- | --- | --- |
| `tdd` | mattpocock | لایه نرمال‌سازی فارسی باید ۱۰۰% تست داشته باشد. بدون استثنا |
| `code-review` | mattpocock | دومحوره: استاندارد + انطباق با اسپک. قبل از هر کامیت |
| `diagnosing-bugs` | mattpocock | باگ‌های ZWNJ، bidi و رندر فارسی از نوع «دیدنی ولی غیرقابل بازتولید»ند — به حلقه منطقی نیاز دارند |
| `research` | mattpocock | هر ادعای لایسنس/ریپو باید به فایل مارکداون منبع‌دار تبدیل شود. تله n8n/Dify دوباره تکرار نشود |
| `ponytail` (سطح full) | DietrichGebert (MIT) | بزرگ‌ترین ریسک ما over-engineering قبل از اولین درآمد است. ❗ استثناها را در `.cursor/rules/50-skills-workflow.mdc` بخوان |
| `writing-great-skills` | mattpocock | چون ما باید اسکیل فارسی خودمان را بنویسیم — هیچ‌کس ننوشته |
| `mcp-builder` | skills.sh | معماری ما MCP-first است. این اسکیل مستقیماً به قلب محصول می‌خورد |

### ابزار (اسکیل نیست، ولی مرحله ۱)
| ابزار | منبع | دلیل |
| --- | --- | --- |
| `codebase-memory-mcp` | DeusData (MIT) | گراف ساختاری کد، ~۱۲۰ برابر توکن کمتر. **۱۰۰% لوکال، بدون API key** — برای ما دو مزیت: مستقل از تحریم، و کد مشتری جایی نمی‌رود |
| `skills.sh` CLI | skills.sh | مکانیزم نصب. خودش اسکیل نیست |

## مرحله ۴ — فقط وقتی صف بازبینی را می‌سازیم

الان نصب نکن. کانتکست را بی‌دلیل پر می‌کنند.

| اسکیل | منبع | کاربرد |
| --- | --- | --- |
| `pick-ui-library` | emilkowalski (MIT) | جلوگیری از دست‌ساز کردن toast/dialog/diff-viewer |
| `emil-design-eng` + `review-animations` | emilkowalski | صف تأیید باید سریع و قابل اعتماد حس شود، وگرنه بازبین کورکورانه approve می‌کند |
| `webapp-testing` / `playwright-cli` | skills.sh | تست رندر RTL و تست انتشار روی وردپرس داکر |

## مردود — و دلیلش

| منبع | حکم | دلیل |
| --- | --- | --- |
| `impeccable.style` | موکول | لایسنس روی سایت اعلام نشده، نصب از مارکت‌پلیس است و یک «roll API» شبکه‌ای دارد. مهم‌تر: ۵۹ قاعده slop آن برای تایپوگرافی لاتین نوشته شده — درباره RTL، نیم‌فاصله و فونت فارسی صفر می‌داند |
| `ibelick/ui-skills` | موکول | MIT و معتبر، ولی هم‌پوشانی کامل با emilkowalski دارد. یکی را انتخاب کن، نه هر دو |
| `agency-agents` (اکثریت) | مردود | پرسونامحور است نه فرایندمحور. «whimsy injector» توکن مصرف می‌کند بدون اینکه دقت مهندسی بیاورد |
| `seo-audit` / `ai-seo` / `programmatic-seo` | فقط خواندن | ❗ خطرناک. فرضیاتشان وب انگلیسی است. اگر نصب شوند، ایجنت فکر می‌کند موضوع را می‌شناسد و لایه فارسی را دور می‌زند. چک‌لیستشان را بخوان و اقتباس کن، نصب نکن |

## اسکیل‌هایی که هیچ‌کس ننوشته و ما باید بنویسیم

این مزیت رقابتی واقعی است. در `.cursor/skills/` نسخه اولیه‌شان گذاشته شده:

1. `persian-seo-normalization` — قواعد ZWNJ، ی/ك، ezafe، دوفرمی بودن متن
2. `approval-gate` — هر نوشتن بیرونی چه مسیری دارد
3. `persian-content-quality` (بعداً) — گِیت کیفیت محتوای فارسی و E-E-A-T

با `writing-great-skills` بهبودشان بده. بعد از پایدار شدن، اوپن‌سورسشان کن — اعتبار فنی و استانداردسازی بازار می‌آورد.
