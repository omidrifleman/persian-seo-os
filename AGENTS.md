# AGENTS.md — قواعد کار برای هر ایجنت کدنویس (Cursor / Claude Code / Codex)

> این فایل قانون است، نه پیشنهاد. اگر تغییری با این فایل در تضاد است، اول این فایل را به‌روز کن و تأیید انسانی بگیر.

## محصول
پلتفرم سئو و مارکتینگ محتوایی مبتنی بر AI برای **وب فارسی و بازار ایران**.
پوزیشنینگ: **«خودکار با تأیید انسانی» (human-in-the-loop)**.
عبارت «کاملاً خودکار» در کد، UI، مستندات و بازاریابی ممنوع است — سیاست `scaled content abuse` گوگل دقیقاً همان را هدف گرفته است.

## قواعد غیرقابل‌مذاکره
1. **MCP-first** — هر قابلیت ابتدا یک MCP tool با ورودی/خروجی تایپ‌شده (Pydantic) است. UI و ایجنت‌ها فقط مصرف‌کننده‌اند.
2. **هیچ نوشتنی روی سایت مشتری بدون رکورد `approvals` مجاز نیست.** این محدودیت در لایه دیتابیس اجبار می‌شود (`publish_jobs.approval_id` NOT NULL + FK)، نه در UI.
3. همه عملیات نوشتن **idempotent** و **قابل rollback** با `snapshot` قبل از تغییر.
4. **audit log کامل**: چه کسی/چه ایجنتی، کِی، چه چیزی، چرا.
5. **ارکستراسیون در کد** (LangGraph یا کد خودمان + صف کاری). استفاده از n8n یا Dify در مسیر production ممنوع است (لایسنس Sustainable Use و بند منع multi-tenant).
6. **هیچ فراخوانی مستقیم به SDK یک LLM provider** در کد بیزنس مجاز نیست. فقط از `services/llm_gateway` عبور کن.
7. لایه نرمال‌سازی فارسی هسته مزیت رقابتی است: هر تابع عمومی باید تست واحد داشته باشد.

## روش کار در هر تسک
1. ابتدا **طرح اجرا** بده: فایل‌ها، اسکیمای دیتابیس، قرارداد toolها، Definition of Done. منتظر تأیید بمان.
2. بعد کد بنویس، اجرا کن، خروجی تست را نشان بده.
3. هر ابزار/ریپوی اوپن‌سورس پیشنهادی باید همراه باشد با: URL گیت‌هاب + لایسنس دقیق + تاریخ آخرین کامیت + یک جمله دلیل. اگر از وجودش مطمئن نیستی، پیشنهادش نده.
4. اگر تصمیم به اطلاعات تجاری نیاز دارد (بودجه، CMS مشتری، محل هوست)، بپرس؛ حدس نزن.

## دستورهای پرکاربرد
```bash
make install     # نصب وابستگی‌ها
make test        # اجرای تست‌ها
make up          # بالا آوردن postgres/redis
make migrate     # اجرای مایگریشن‌ها
make lint        # فرمت و لینت
```

## چیزهایی که هرگز نباید انجام دهی
- نوشتن روی وردپرس مشتری در تست‌ها یا اسکریپت‌های dev.
- کپی کد از ریپوهای AGPL/GPL (مثل Notipo، Crowl، Mautic) داخل این ریپو. فقط الگو برداری کن.
- کامیت کردن کلید API. همه‌چیز از `.env` می‌آید.
- تولید انبوه محتوا بدون گِیت کیفیت و throttle دامنه.
